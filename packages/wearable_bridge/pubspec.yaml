name: habitar_wearable_bridge
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  habitar_domain:
    path: ../domain
  habitar_routine_engine:
    path: ../routine_engine

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
